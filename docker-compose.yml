services:
  api:
    build: ./server
    ports:
      - "8080:8080"
    env_file:
      - ./server/.env
    restart: unless-stopped

  web:
    build: .
    ports:
      - "8088:80"
    environment:
      - VITE_API_URL=http://localhost:8080
    depends_on:
      - api
    restart: unless-stopped
